# OpenAI — optional. App works with intelligent fallback responses when unset.
OPENAI_API_KEY=

# Supabase — optional. App falls back to in-memory store when unset.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
